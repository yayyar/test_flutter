import 'package:flutter/material.dart';

class FoodController with ChangeNotifier {
  // 1. get data from database service
  // and show in UI

  // 2. insert data to database service
  // call insertData method from UI widget
}
